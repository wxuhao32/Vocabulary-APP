# Changelog

> 本项目版本号规则：`v9.x`（x 为功能/修复递增）。
> 此前 `versionName` 长期停留在 `8.1`，自 **v9.77** 起随构建同步更新。

---

## v9.123 — 2026-08-14

**语音消息接收兼容 + 登录 Token 有效期调整**

- 语音消息接收兼容：补全整条链路（录音 → 上传 → DB → WS → 接收方渲染），服务端 + 前端源码均正确（`DB type=voice`、历史返回 `{type:"voice", duration:N}` 标记）。接收方显示纯数字 `"3"` 的根因是接收方设备装了旧版本 APK（v9.119 无语音条分支），走 `else` 文本分支渲染为 `content`。
- 兼容改造：voice 消息 `content` 由纯数字 `"3"` 改为可读文本 `"[语音] 3秒"`。旧版前端（无语音条分支）会显示为"语音 3秒"而非裸数字；新版前端 `_msg_dict` 解析兼容（先 `int(content)` 失败再正则 `\d+` 兜底）。历史数据 `content='3'/'2'` 仍正确解析 `duration`。
- 验证：后端 6/6 PASS；前端 B 历史加载显示语音条 ✅ / 关闭重开仍在 ✅。

---

## v9.122 — 2026-08-14

**语音消息录音初始化继续修复：`NotReadableError` 自动重试 + 真实原因区分 + 开发者日志**

- `NotReadableError` 不再中断判定为"被占用"：首次 `getUserMedia` 失败自动重试一次（400ms 后，第二次用降级约束——关闭回声消除/降噪/增益处理等组件），解决 Android WebView 音频管线**首次初始化时序问题**（最常见的"权限已授予但录音失败"真因）。
- 录音格式 / `MediaRecorder` 初始化失败按真实原因处理：MediaRecorder 构造失败（mime 依次 `webm/opus → webm → mp4 → mp4a.40.2` 逐级探测，全失败）→"设备不支持当前录音格式参数"；`mr.start()` 在 try → 失败提示"录音器启动失败（真实错误名）"；新增 `mr.onerror` 监听 → 录音中错误显示真实错误名。
- 开发者日志：`voiceLog()` 记录完整录音链路（`getUserMedia` 尝试次数 / 约束 / 错误名 / 消息 / MediaRecorder mime 选择 / 构造与 start 结果 / stop 时长大小）；`localStorage cet4deck.voice_debug=1` 开启屏幕上调试图（带字小窗，可截图反馈）。
- Manifest 添 `MODIFY_AUDIO_SETTINGS`（部分设备 WebView 录音需调整音频路由）。

---

## v9.117 ~ v9.121 — 2026-08-13

**聊天会话体验优化 / 系统通知栏 / 语音消息**

- v9.117：发送后输入法保持展开 / 文件上传圆形进度环 + 失败重试 / 图片发送本地立显 / 文件下载进度反馈 + 真机保存到设备。
- v9.118：系统通知栏完善 + 修复跨会话泄漏（`pending/ack` 会话成员过滤 + 排除已读 + 服务通知低调化）。
- v9.119：语音消息（按开闭原则扩展——复用文件上传消息持久化结构 + WS 实时通道），新增 `mic/pause/keyboard` 工具图标 + 录音条 UI + `MediaRecorder` 录制 + 上滑取消 + 语音条播放缓冲。
- v9.121：内部优化。

---

## v9.110 ~ v9.116 — 2026-08-12

**消息通信升级为 WebSocket 实时**

- v9.110：消息通信升级为 WebSocket 实时（登录建立 → 自动重连 / 离线补同步）。
- v9.111：滑块验证码稳定性修复（拖手坐标重算 + 服务端调试日志 + IPv6 适配）。
- v9.112：消息系统通知服务（原生 WS + 前台服务，App 后台 / 离线时系统通知栏推送，独立于 WebView）。
- v9.113：聊天体验优化（软键盘适配 / 表情发送状态文字可读 + @对方）。
- v9.114：自己消息深色文字 + 键盘跟随双信号 + 通知栏格式修复。
- v9.115：日志诊断模式（消息生命周期 / 通知 / 键盘全链路日志上报服务端）。
- v9.116：内部优化。

---

## v9.93 ~ v9.109 — 2026-08-10 ~ 2026-08-12

**社交 / 通知 / 封禁 / 管理员**

- v9.93：独立管理员系统（隐藏 + 双因素认证 + Admin Dashboard + 用户封禁）。
- v9.94：通知中心（9 条上下文滑加 + 单条已读 + 全局页面返回栈）。
- v9.95：封禁扩展（时长 / 原因 / 自动解封）+ 客户端版本更新检测（可选强制）。
- v9.96：通知左滑删除 + 聊天记录删除 / 清空 + 聊天背景 + 头像裁剪。
- v9.97：版本更新链路修复（原生下载安装框 + 每次启动检查）。

---

## v9.85 ~ v9.92 — 2026-08-06 ~ 2026-08-10

**接入本地认证 / 社交模块**

- v9.85：接入真实本地认证（FastAPI + SQLite + JWT）。
- v9.86：登录失败限流 + 密码强度校验（防暴力枚举）。
- v9.87：社交模块（好友 / 私聊 / 文件 / 通知）。
- v9.88：修复登录页 tab 切换失效。
- v9.89：UI / UX 全局优化 + 社交增强。
- v9.90：社交 UI 细节修正。
- v9.91：系统通知栏 + 消息撤回 + 消息已读。
- v9.92：修复聊天头像显示。

---

## v9.77 ~ v9.84 — 2026-08-01 ~ 2026-08-06

**基础建设 / 同步版本号**

- v9.77：版本号起点（同步 CHANGELOG 至此开始随构建更新）。
- v9.80：本地模型保活修复。
- v9.81：同步 CHANGELOG 版本（9.77 → 9.81），让用户能区分新旧包。
- v9.82：修复两个显示 bug（日期 / 开始背单词按钮数字）。
- v9.83：账户中心 / 登录 UI（增强 1 级前端）。
- v9.84：登录 / 账户页返回键支持 + 返回箭头统一。

---

> 详细提交记录见 git history（`git log --oneline`）。
